define ([
    'jquery',
    'underscore',
    'backbone',
    'joint',
], function ($, _, Backbone, joint) {
	var editPanelModel = Backbone.Model.extend({
		initialize: function() {
		}
	});
	return editPanelModel;
});
