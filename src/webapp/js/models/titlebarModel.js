define ([
    'jquery',
    'underscore',
    'backbone',
    'joint',
], function ($, _, Backbone, joint) {
	var titlebarModel = Backbone.Model.extend({
		initialize: function() {
		}
	});
	return titlebarModel;
});
