define ([
    'jquery',
    'underscore',
    'backbone',
    'joint',
], function ($, _, Backbone, joint) {
	var pathModel = Backbone.Model.extend({
		initialize: function() {
		}
	});
	return pathModel;
});
